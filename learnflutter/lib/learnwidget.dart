import 'package:flutter/material.dart';

class Learnwidget1 extends StatelessWidget {
  const Learnwidget1({Key key}) : super(key: key);
//
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Learnwidget1stclass"),
        ),
        body: Column(
          children: [
            Text("Bokkor"),
            Text("Shiblo"),
            Text("Fahim"),
          ],
        ));
  }
}

//child-1

//children-jto ta kusi...
